import React from "react";
import ReactDOM from "react-dom/client";
import { ChromaImpactGrid } from "./ChromaImpactGrid";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ChromaImpactGrid />
  </React.StrictMode>
);
